package com.maghrebia.User.profiling.risk;

import com.maghrebia.User.profiling.userP.UserProfile;
import org.springframework.stereotype.Service;

@Service
public class RiskScoringService {
    /*
    public double calculateRiskScore(UserProfile profile) {
        double score = 0;

        // Critères de risque
        if (profile.getAge() < 25) score += 20;
        if (profile.isHasCar() && profile.getAccidentHistory() > 2) score += 30;
        if (profile.isSmoker()) score += 10;
        if (profile.isHasMedicalHistory()) score += 15;

        return Math.min(score, 100); // Score max = 100



    } */
}

