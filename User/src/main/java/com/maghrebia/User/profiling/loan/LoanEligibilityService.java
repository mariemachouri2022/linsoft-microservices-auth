package com.maghrebia.User.profiling.loan;

import com.maghrebia.User.profiling.userP.UserProfile;
import org.springframework.stereotype.Service;

@Service
public class LoanEligibilityService {
   /* public boolean isEligibleForLoan(UserProfile profile) {
        if (profile.getIncome() < 2000) return false; // Salaire minimum pour emprunter
        if (profile.getCreditScore() < 50) return false; // Score de crédit minimum
        return true;
    }*/
}

