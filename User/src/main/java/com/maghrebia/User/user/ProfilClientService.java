package com.maghrebia.User.user;

import org.springframework.stereotype.Service;

@Service
public class ProfilClientService {
/*
    public String evaluerCapaciteEmprunt(ProfilClient client) {
        double ratioEndettement = client.getChargesMensuelles() / client.getRevenuMensuel();

        if (ratioEndettement < 0.3 && client.getScoreCredit() > 650) {
            return "Apte à emprunter";
        } else if (ratioEndettement < 0.4 && client.getScoreCredit() > 550) {
            return "Éligible sous conditions";
        } else {
            return "Non éligible";
        }
    }

    public String evaluerRisqueAssurance(ProfilClient client) {
        double risque = client.getNombreAccidents() * 0.4 + client.getNombreSinistres() * 0.3;

        if (risque < 1) {
            return "Risque faible - Prime basse";
        } else if (risque < 3) {
            return "Risque moyen - Prime standard";
        } else {
            return "Risque élevé - Prime élevée";
        }
    }*/
}

